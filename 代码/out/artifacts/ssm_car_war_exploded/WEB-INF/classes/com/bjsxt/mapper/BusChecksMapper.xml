<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bjsxt.mapper.BusChecksMapper">

    <insert id="insert">

        insert  into  Bus_Checks  values(#{checkid},#{checkdate},#{checkdesc},#{problem},#{paymoney},#{opername},#{rentid})

    </insert>


</mapper>